export const metadata = {
  title: 'Under Maintenance | ZennithDevelopments',
  description: 'We are currently performing scheduled maintenance. We\'ll be back soon!',
};

export default function MaintenanceLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
